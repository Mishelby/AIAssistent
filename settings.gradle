pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url = 'https://repo.example.com' }
    }
}

rootProject.name = 'AIAgent'
include 'main', 'core', 'infrastructure-kafka', 'api', 'common'

