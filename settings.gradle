pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url = 'https://repo.example.com' }
    }
}

rootProject.name = 'AIAgent'
include 'core', 'infrastructure-kafka'
